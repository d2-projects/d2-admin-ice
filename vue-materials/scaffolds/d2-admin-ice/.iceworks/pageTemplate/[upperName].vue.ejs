<template>
  <!-- d2-container 文档 http://d2admin.fairyever.com/zh/components/container.html -->
  <d2-container class="page-<%= name %>">
    <template slot="header"><%= name %></template>
    <%_ blocks.forEach(function(block) { _%>
    <<%= block.name %>/>
    <%_ }); _%>
    <template slot="footer">自动生成 <%= blocks.length %> 个组件</template>
  </d2-container>
</template>

<script>
// mixin
import mixinPage from '@/mixins/page'
// 组件
<%_ blocks.forEach(function(block) { _%>
import <%= block.className %> from '<%= block.path %>'
<%_ }); _%>
// 本页面 请删除你不需要的预置代码
// 由于 iceworks 组件代码风格和 D2Admin 不一致，ESLint 会报错误提示
// 如果介意的话可以运行 npm run lint 自动修复
export default {
  name: '<%= name %>',
  components: {
    <%_ blocks.forEach(function(block, index) { _%>
    <%= block.className %><%= index !== blocks.length - 1 ? ',' : '' %>
    <%_ }); _%>
  },
  // 组合
  mixins: [
    'mixinPage'
  ],
  // 数据
  data () {
    return {}
  },
  // 计算属性
  computed: {},
  // 侦听器
  watch: {},
  // 生命周期钩子
  beforeCreate () {},
  created () {},
  beforeMount () {},
  mounted () {},
  beforeUpdate () {},
  updated () {},
  activated () {},
  deactivated () {},
  beforeDestroy () {},
  destroyed () {},
  // 事件处理方法
  methods: {}
}
</script>

<style lang="scss" scoped>
@import './<%= upperName %>.scss';
</style>
